PUT obras
{
  "settings" : {
        "number_of_shards" : 1,
        "number_of_replicas" : 0
    },
  "mappings":
  {
    "Pintura":
    {
      "properties":
      {
        "nome": { "type": "text"},
        "titulo": {"type":"text"},
        "descricao": {"type":"text"},
        "dataCadastro": {
          "type": "date",
          "format": "dd/MM/yyyy HH.mm.ss||dd/MM/yyyy"
        },
        "dataPublicacao": {
        "type": "date",
        "format": "dd/MM/yyyy"
        },
        "dataAquisicao": {
        "type": "date",
        "format": "dd/MM/yyyy"
        }
      }
    },
    "Arquitetura":
    {
      "properties":
      {
         "nome": { "type": "text"},
        "titulo": {"type":"text"},
        "descricao": {"type":"text"},
        "dataCadastro": {
          "type": "date",
          "format": "dd/MM/yyyy HH.mm.ss||dd/MM/yyyy"
        },
        "dataPublicacao": {
        "type": "date",
        "format": "dd/MM/yyyy"
        },
        "dataAquisicao": {
        "type": "date",
        "format": "dd/MM/yyyy"
        }
      }
    },
    "Escultura":
    {
      "properties":
      {
        "nome": { "type": "text"},
        "titulo": {"type":"text"},
        "descricao": {"type":"text"},
        
        "dataCadastro": {
          "type": "date",
          "format": "dd/MM/yyyy HH.mm.ss||dd/MM/yyyy"
        },
        "dataPublicacao": {
        "type": "date",
        "format": "dd/MM/yyyy"
        },
        "dataAquisicao": {
        "type": "date",
        "format": "dd/MM/yyyy"
        }
      }
    }
    
  }
}